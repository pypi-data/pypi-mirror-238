[tool.poetry]
name = "brickworks"
version = "0.0.1"
description = ""
authors = ["jens <jens.kuerten42@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.104.0"
sqlalchemy = "^2.0.22"
alembic = "^1.12.0"
pydantic-settings = "^2.0.3"
aiosqlite = "^0.19.0"
typer = "^0.9.0"
tomli = "^2.0.1"
httpx = "^0.25.0"

[tool.poetry.scripts]
mason = 'brickworks.cli:main'

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.brickworks]
settings = "tests.cli.fixtures.settings.settings"
