[tool.poetry]
name = "Dirlin"
version = "0.1.3"
description = "Handy functions for local files"
authors = ["mendozasho"]
readme = "README.md"

packages = [{include = "directory_handler"}]

[tool.poetry.dependencies]
python = "^3.11"
pandas = "2.1.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
