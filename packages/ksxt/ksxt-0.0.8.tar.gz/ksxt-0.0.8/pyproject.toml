[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]

build-backend = "setuptools.build_meta"

[project]
name = "ksxt"
version = "0.0.8"
readme = "README.md"
license = { file="LICENSE.txt" }

# pytest path 설정
[tool.pytest.ini_options]
pythonpath = [
    ".", "src",
]