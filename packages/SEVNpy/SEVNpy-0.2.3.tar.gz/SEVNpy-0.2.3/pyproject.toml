[build-system]
requires = ["setuptools", "wheel", "numpy", "typing_extensions", "pandas", "scipy"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]