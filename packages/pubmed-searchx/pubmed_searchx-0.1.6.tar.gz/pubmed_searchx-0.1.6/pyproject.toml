[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
[project]
name = "pubmed_searchx"
version = "0.1.6"
authors = [
  { name="AnhND", email="nguyenanh.nda@gmail.com" },
]
description = "Pubmed Search"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

dependencies = [
    'beautifulsoup4 >= 4.12.2',
    'lxml >= 4.9.3',
    'requests >= 2.31.0',
    'tqdm >= 4.66.1'
]


#[project.urls]
#"Homepage" = ""
#"Bug Tracker" = ""