from setuptools import setup

setup(
    name="cym-minimal-package",
    version="0.1",
    author="Aco",
    description="Cym's minimal Python package",
    packages=["my_minimal_package"],
    install_requires=["numpy"],
)
