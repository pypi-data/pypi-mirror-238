[tool.poetry]
name = "kyle-portal-gun"
version = "0.1.0"
description = ""
authors = ["Kyle Bates <jkbates10@outlook.com>"]
readme = "README.md"

[tool.poetry.scripts]
kyle-portal-gun = "kyle_portal_gun.main:app"

[tool.poetry.dependencies]
python = "^3.12"
typer = {extras = ["all"], version = "^0.9.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
