from setuptools import setup, find_packages

setup(
    name='schoginitoys',
    version='1.1',
    packages=find_packages(),
    install_requires=[
        # list your project dependencies here
    ],
)

