from setuptools import find_packages, setup

setup(
    name='Dodrio-local-server',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # list your dependencies here
    ],
)