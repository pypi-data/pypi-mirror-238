[tool.poetry]
name = "TUOP"
version = "0.0.1"
description = "for ege"
authors = ["UshliyOrk"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">= 3.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
