[tool.poetry]
name = "mantium-spec"
version = "1.0.1108"
description = "OpenAPI-generated Python client"
authors = ["Mantium <support@mantiumai.com>"]
readme = "README.md"
packages = [{include = "mantium_spec"}]

[tool.poetry.dependencies]
python = ">=3.7"
openapi-client = "^1.1.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
